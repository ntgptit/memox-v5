Status pill — SRS lifecycle + due workload + generic states. Colour is paired with a label; Due (amber) never reads as Wrong (red).

```jsx
<MxBadge tone="due">Due 83</MxBadge>
<MxBadge tone="preSrs">Box 0</MxBadge>
<MxBadge tone="mastered">Box 8</MxBadge>
```

Tones: `preSrs`, `active`, `due`, `noDue`, `mastered`, `lapse`, `correct`, `wrong`, `info`, `neutral`.
