A tile in matchMode. Grade a pair only after two tiles are selected.

```jsx
<MxMatchTile side="prompt" state="selected">기자</MxMatchTile>
<MxMatchTile side="meaning" state="correct">Reporter</MxMatchTile>
```

States: `default`, `selected`, `correct`, `wrong`, `completed` (locked/faded).
