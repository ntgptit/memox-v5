Underline filter tabs. Use for content filters (All / due / new); use MxSegmentedControl instead for a compact 2–3 option pill switch.

```jsx
<MxTabs options={["All", "Due", "New", "Mastered"]} value={tab} onChange={setTab} />
```
