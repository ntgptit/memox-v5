Bazar-style header. `size="hero"` for a screen intro (grey eyebrow + big purple heading); default for a section row with "See all".

```jsx
<MxSectionHeader size="hero" eyebrow="Keep going" title="Your decks" />
<MxSectionHeader title="Continue studying" actionLabel="See all" onAction={openAll} />
```
